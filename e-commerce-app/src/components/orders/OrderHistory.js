import React from 'react';

const OrderHistory = () => {
    return (
        <div>
            <h2>Order History</h2>
            {/* Render order history here */}
        </div>
    );
};

export default OrderHistory;
